app = "bahaya"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[processes]
  app = "python -O bot.py" # Jalanin bot doang, gak perlu web

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
