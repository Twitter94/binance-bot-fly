    app = "bahaya"
    primary_region = "sin"

    [build]
      dockerfile = "Dockerfile" # karena repo ini pake docker

    [env]
      TZ = "Asia/Jakarta"

    [processes]
      app = "python main.py" # <--- PENTING. harus main.py
