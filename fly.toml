app = "bahaya"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  memory = "256mb" # INI KUNCINYA. DARI 512 JADI 256
  cpu_kind = "shared"
  cpus = 1 # 1 cpu shared

[processes]
  app = "python bot.py"
