app = "bahaya"
primary_region = "sin"

[build]

[processes]
  app = "python main.py"

[[services]]
  internal_port = 8080
  protocol = "tcp"
  auto_stop_machines = false
  auto_start_machines = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]
