app = "bahaya"
primary_region = "sin" # Singapore biar deket ke Binance

[build]
  builder = "paketobuildpacks/builder:base"

[env]
  # KOSONGIN AJA

[[services]]
  internal_port = 8080
  protocol = "tcp"
